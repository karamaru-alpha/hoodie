version: "3.8"

services:
  buf:
    build:
      context: .
      dockerfile: build/buf.Dockerfile
      args:
        - GO_VERSION=${GO_VERSION}
    working_dir: /usr/src/days
    volumes:
      - ./:/usr/src/days
